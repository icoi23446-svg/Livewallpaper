<resources>

    <!-- الثيم الرئيسي للتطبيق -->
    <style name="Theme.UltimateLiveWallpaper" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:buttonStyle">@style/Widget.CustomButton</item>
    </style>

    <!-- أزرار مخصصة -->
    <style name="Widget.CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorButtonText</item>
        <item name="android:padding">12dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- TextViews (العناوين فوق كل عنصر) -->
    <style name="Widget.CustomLabel" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">8dp</item>
    </style>

</resources>
